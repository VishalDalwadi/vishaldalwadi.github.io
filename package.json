{
  "name": "paperplanes.cloud",
  "type": "module",
  "version": "0.0.1",
  "private": true,
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "scenes:build": "node scripts/build-scenes.mjs",
    "new-post": "node scripts/new-post.mjs",
    "prebuild": "npm run scenes:build",
    "predev": "npm run scenes:build",
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.3",
    "astro": "^7.0.6"
  }
}
